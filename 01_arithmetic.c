#include<stdio.h>

int main(){
    int a = 3;
    int b = 5;
    int c = a+b ;
    printf("the a is %d and b is %d and sum is %d\n" ,a,b,c);
    // modulus is used for finding remainder;
    printf("the a is %d and b is %d and a is divided by b and the remainder is %f" ,a,b,a%b);
    return 0;
}